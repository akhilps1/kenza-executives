enum ForgetPasswordFormViewState {
  phone,
  otp,
  password,
}

enum TransactionStatus {
  online,
  offline,
}

enum TransactionType {
  debit,
  credit,
}

enum ModeOfPay {
  self,
  branch,
  executive,
}
